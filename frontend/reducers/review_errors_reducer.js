

const ReviewErrorsReducer = (state = [], action) => {
  Object.freeze(state);
  switch (action.type) {
    case expression:

    default:
      return state;
  }
};

export default ReviewErrorsReducer;
